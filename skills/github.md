# Skill: GitHub (gh)

Use for repo ops, issues/PRs, CI runs, and lightweight reviews.

## Install
```bash
openclaw skill install github
```

## Typical usage
```bash
gh auth status

gh repo create <owner>/<repo> --public --source=. --push

gh pr list --limit 10
```
